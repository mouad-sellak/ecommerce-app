@model LocationVoiture.Models.Marque
@{
    Layout = "~/Views/Shared/_OwnerDashboard.cshtml";
}

<div class="m-5">
    <h2>@LocationVoiture.Resources.most.details</h2>

    <div>
        <h4>@LocationVoiture.Resources.Models.MarqueModel.libele</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.libele)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.libele)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.date_ajout)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.date_ajout)
            </dd>

        </dl>
    </div>
    <p>
        @if (User.IsInRole("Owner"))
        {
            @Html.ActionLink(@LocationVoiture.Resources.most.edit, "Edit", new { id = Model.id_marque })

        }
        | @Html.ActionLink(@LocationVoiture.Resources.most.back, "Index")
    </p>
</div>